<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.FavoriteMapper">
    <resultMap id="result" type="persistence.dto.FavoriteDTO">
        <id column="PNO" property="no" />
        <result property="user_id" column="user_id"/>
        <result property="transaction_id" column="transaction_id"/>
    </resultMap>
    <select id="selectAll" resultMap="result">
        select * from Favorite;
    </select>
    <update id="updateAll">
        update Favorite set user_id = #{user_id} , transaction_id = #{transaction_id}
    </update>
    <insert id="insert">
        insert into Favorite (user_id, transaction_id) values (#{admin_id}, #{transaction_id})
    </insert>
    <delete id="delete">
        delete from Favorite
        where PNO = #{no}
    </delete>
</mapper>
